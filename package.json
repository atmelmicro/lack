{
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.511.0",
    "@aws-sdk/lib-dynamodb": "^3.511.0",
    "@prisma/client": "^5.9.1",
    "aws-cdk-lib": "^2.127.0",
    "better-sqlite3": "^9.4.0",
    "drizzle-orm": "^0.29.3",
    "esbuild": "^0.20.0",
    "prisma": "^5.9.1",
    "uuid": "^9.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "^1.0.5"
  },
  "scripts": {
    "deploy": "bun run build && cdk deploy --app 'node deploy.js' --profile admin",
    "build": "bun run build.ts"
  }
}
